# Required for the summarizer and enhancer agent workflows.
OPENAI_API_KEY=

# Optional upstream auth for FastMCP bootstrap and live endpoint checks.
UPSTREAM_BEARER_TOKEN=
UPSTREAM_API_KEY=
UPSTREAM_API_KEY_HEADER=api_key
